# CLAUDE.md — 第五人格風ゲーム 開発ガイド

## プロジェクト概要

第五人格（Identity V）風の2Dトップダウン非対称マルチプレイヤーホラーゲーム。
1人のハンターが4人のサバイバーを追いかけ、サバイバーは暗号機を解読して脱出する。

## 現在の実装状況

- [x] Node.js + Express サーバー
- [x] Phaser.js 3 による800x600の2Dトップダウンマップ表示
- [x] WASDで動く円形キャラクター（斜め移動正規化済み）
- [x] **Socket.io によるオンラインマルチプレイ（サーバー権威）**
- [x] **役職自動割当**（最初の接続=ハンター、以降=サバイバー、最大4人）
- [x] **暗号機 5台ランダム配置 + 解読プログレスバー**（Fキー保持で進行）
- [x] **脱出ゲート**（5台解読で開放、サバイバーがゲート領域に入ると脱出）
- [x] **ハンター移動速度 1.3倍**
- [x] **クリック攻撃**（範囲内のサバイバーに1ダメージ、1秒クールダウン）
- [x] **2回攻撃でダウン → Fキーで担ぐ → チェアに運んでFキーで拘束**
- [x] **チェア拘束カウントダウン（30秒）→ 脱落**
- [x] **救助メカニクス**（他サバイバーがチェア横でFキー保持で救助）
- [x] **勝敗判定**（脱落3人=ハンター勝利 / 全員脱落判定 + 脱出者あり=サバイバー勝利）

## 開発コマンド

```bash
npm install   # 初回セットアップ
npm start     # サーバー起動 → http://localhost:3000
```

## 動作確認方法

1. `npm start` でサーバー起動
2. ブラウザで `http://localhost:3000` を開く（最初のタブ＝ハンター）
3. 別タブ／別ブラウザで同 URL を開く（2つ目以降＝サバイバー、最大4人）
4. 1ハンター＋1サバイバー揃った時点で `playing` に遷移しゲーム開始
5. 全員が切断されるとゲーム状態がリセットされる

## 操作方法

### 共通
- `WASD` — 移動
- 自分のキャラには黄色い縁取りが表示される

### サバイバー（白円）
- 暗号機（茶色丸）に近づいて `F` 保持 → 解読（青いプログレスバー）
- チェア（茶色四角）に拘束された仲間の横で `F` 保持 → 救助（青いプログレスバー）
- 5台解読後、画面右の緑色ゲートに入ると脱出

### ハンター（赤円）
- 範囲内のサバイバーをクリックで攻撃（1秒クールダウン）
- ダウンしたサバイバー（赤×印）の横で `F` → 担ぐ（移動速度低下）
- チェアの横で `F` → 拘束（赤いカウントダウンバー、30秒で脱落）

## UI

| 位置 | 要素 | 仕様 |
| --- | --- | --- |
| 左上 | ステータステキスト | 自分のRole/State/HP、Eliminated/Escaped/Players |
| 上中央 | **残り暗号機カウンタ** | 黄色大文字。ゲート開放後は緑＋"GATE OPEN" |
| 下中央 | 操作ヒント | ロール別の操作ガイド |
| 右下 | **ミニマップ (160x120)** | マップ全体を1/5縮小。暗号機(緑/黄)、チェア、ゲート、プレイヤー(自分=黄+黒縁、他=白、ハンター=赤、ダウン=暗赤)を点で表示。視界制限なし。 |
| 画面全体 | **Spotted警告** | 自分が survivor でハンターから半径 `SPOTTED_RANGE` (200px) 以内にいる間、赤縁＋"WARNING — HUNTER NEARBY"を約1Hzで点滅。`spotted` フラグはサーバーが算出 |
| 画面中央 | **リザルト画面** | 勝敗確定で全画面暗転。勝者大文字 / 「You Win!」or「You Lose...」/ 統計 (decoded・escaped・eliminated) / リロード案内。`gameover` イベントが届かなかった場合の保険として `state.status` も併用 |

## ファイル構成

```
server.js              # Express + Socket.io、30Hzゲームループ、サーバー権威ロジック
public/index.html      # Phaser.js + Socket.io クライアントCDN
public/js/game.js      # 入力送信・サーバー状態描画・UI
```

## アーキテクチャ方針

- **サーバー権威（source of truth）**：クライアントは入力（WASD/F/click）のみ送信。サーバーが30Hz（`TICK_MS`）で物理・ロジックをシミュレートし、座標・HP・状態・暗号機進捗・チェア状態・勝敗のすべてを単一の真実として保持する。
- **不正防止**：
  - 位置・HP・解読進捗はサーバー内のみで更新。クライアントから受信するのは入力フラグのみで、`p.input.up/down/left/right/action` は `!!` で boolean に正規化、`click` は型チェック付きで取り込む。
  - 攻撃成立判定はサーバーの `hunter.x/y` と `survivor.x/y` で行うため、クライアントが偽の click 座標を送っても範囲外なら無効。
  - 解読・救助は「サーバーが毎tick距離計算 → 範囲内なら進捗加算」。クライアントが「解読完了」を主張する手段は存在しない。
- **同期サイクル**：
  - `setInterval(tick, 33ms)` … 30Hzシミュレーション（精度確保）。
  - `setInterval(broadcast, 100ms)` … 全クライアントへ `state` イベントで全状態（players座標、HP、状態、ciphers進捗、chairs、gate、count類）を送信。
  - `gameover` イベント … 勝敗確定tickで `io.emit('gameover', { winner, decodedCount, eliminatedCount, escapedCount })` を即時1回送出。
- **クライアント補間**：100ms間隔broadcastだとプレイヤー位置がカクつくため、直前/最新スナップショット間で線形補間（`interpolatedPos`）。`down`/`on_chair` 状態のキャラはサーバー側でピン留めされているので補間しない。
- **単一グローバルゲーム**：ルーム概念なし。1サーバー＝1ゲーム。
- **マップは矩形 + 境界クランプのみ**：壁・タイルマップ・障害物は未実装。

## ネットワークイベント一覧

| 方向 | イベント名 | ペイロード | 頻度 |
| --- | --- | --- | --- |
| C→S | `input` | `{up,down,left,right,action: bool, click: {x,y}\|null}` | クライアント update 毎（≒60Hz） |
| S→C | `init` | `{id, role}` | 接続時1回 |
| S→C | `state` | 全ゲーム状態（players[`{...,spotted}`], ciphers, chairs, gate, counts, status） | 100ms毎 |
| S→C | `gameover` | `{winner: 'survivors'\|'hunter', decodedCount, eliminatedCount, escapedCount}` | 勝敗確定時1回 |

## 主要な調整パラメータ（server.js 上部）

| 定数 | 値 | 意味 |
| --- | --- | --- |
| `TICK_RATE` | 30 | サーバー tick / 秒 |
| `SURVIVOR_SPEED` | 160 | px/s |
| `HUNTER_SPEED` | 208 (= 1.3x) | px/s |
| `CIPHER_DECODE_PER_TICK_PER_DECODER` | 100 / (20 * 30) | 1人で20秒解読 |
| `RESCUE_PER_TICK_PER_RESCUER` | 100 / (5 * 30) | 1人で5秒救助 |
| `CHAIR_COUNTDOWN_TICKS` | 30 * 30 | 30秒で脱落 |
| `ATTACK_RANGE` | 60 | px |
| `ATTACK_COOLDOWN_TICKS` | 30 | 1秒 |
| `ELIMINATE_TARGET` | 3 | ハンター勝利の脱落数 |
| `SPOTTED_RANGE` | 200 | サバイバーが警告状態になるハンターからの距離 (px) |
| `BROADCAST_INTERVAL_MS` | 100 | state ブロードキャスト間隔 |

---

## TODO（未実装）

### フェーズ1: マップ・ビジュアル

- [ ] **タイルマップ実装** — Tiled エディタで作った JSON マップを読み込む (`public/assets/maps/`)
- [ ] **壁・障害物の衝突判定** — サーバー側でタイル衝突チェック
- [ ] **キャラクタースプライト** — 円を実際のスプライトシートに置き換え
- [ ] **カメラ追従** — マップを 800x600 より大きくする際に必要
- [ ] **アニメーション** — 移動方向に応じた歩行アニメ

### フェーズ2: ゲームメカニクス拡張

- [ ] **板・窓** — サバイバーが通過できてハンターが遅延するオブジェクト
- [ ] **ハンター視界制限** — ヒートマップ／視界ぼかしによる位置情報の非対称化
- [ ] **足音可視化** — サバイバーの足音をハンター画面にだけ表示
- [ ] **チェスト** — アイテム取得（治療キット、ロケット花火など）

### フェーズ3: ロビー・マッチメイキング

- [ ] **ルームシステム** — 複数ゲームの並列実行
- [ ] **役職選択画面** — ハンター/サバイバー希望の事前選択UI
- [ ] **マッチング待機ロビー** — 5人揃ったら自動開始
- [ ] **ゲーム終了後の再戦フロー拡張** — 現状は全員の再戦同意で即再開のみ。ルーム継続・ready解除・再役職選択は未対応

### フェーズ4: ゲームシステム

- [ ] **スキル/アビリティ** — ハンター固有能力（チェーン投擲など）・サバイバー固有スキル
- [ ] **ランク・マッチメイキング** — ELOレーティングシステム
- [ ] **サウンド** — BGM・SE（足音、暗号機音、悲鳴）
- [ ] **HUD改善** — 味方サバイバーの状態アイコン、スキルクールダウン表示

### インフラ

- [ ] **環境変数管理** — `.env` + `dotenv` で PORT・DB接続情報を管理
- [ ] **データベース** — プレイヤープロフィール・統計の永続化
- [ ] **本番デプロイ** — Docker化 + Railway / Render へのデプロイ設定
- [ ] **状態の補間/予測強化** — 現状はスナップショット間の線形補間のみで、入力予測や巻き戻し補正は未対応
