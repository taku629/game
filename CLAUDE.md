# CLAUDE.md — 第五人格風ゲーム 開発ガイド

## プロジェクト概要

第五人格（Identity V）風の2Dトップダウン非対称マルチプレイヤーホラーゲーム。
1人のハンターが4人のサバイバーを追いかけ、サバイバーは暗号機を解読して脱出する。

## 現在の実装状況

- [x] Node.js + Express サーバー
- [x] Phaser.js 3 による800x600の2Dトップダウンマップ表示
- [x] WASDで動く白い円形キャラクター（物理エンジン使用、斜め移動正規化済み）

## 開発コマンド

```bash
npm install   # 初回セットアップ
npm start     # サーバー起動 → http://localhost:3000
```

## ファイル構成

```
server.js              # Express静的ファイルサーバー (port 3000)
public/index.html      # Phaser.js CDN読み込み + キャンバス表示
public/js/game.js      # GameScene: マップ描画・プレイヤー制御
```

## アーキテクチャ方針

- サーバーは現状 `express.static` のみ。ゲームロジックはクライアント完結。
- マルチプレイ追加時は Socket.io をサーバーに載せ、サーバーが権威的なゲーム状態を保持する。
- Phaser Scene を機能ごとに分割（UIScene, GameScene, NetworkScene）することを推奨。

---

## TODO（未実装）

### フェーズ1: シングルプレイヤー基盤

- [ ] **タイルマップ実装** — Tiled エディタで作った JSON マップを読み込む (`public/assets/maps/`)
- [ ] **壁・障害物の衝突判定** — TilemapLayer に staticGroup を設定
- [ ] **キャラクタースプライト** — 白い円を実際のスプライトシート（サバイバー/ハンター）に置き換え
- [ ] **カメラ追従** — `this.cameras.main.startFollow(player)` でワールドマップ対応
- [ ] **アニメーション** — 移動方向に応じた歩行アニメ（上下左右4方向）

### フェーズ2: ゲームオブジェクト

- [ ] **暗号機** — マップ上に5台配置。インタラクションキー（E）で解読進行（プログレスバー表示）
- [ [ **脱出ゲート** — 暗号機5台解読後に開放。プレイヤーが通過で勝利
- [ ] **ハンターAI（仮）** — シングルプレイ確認用の簡易追跡AI（A*またはステアリング）
- [ ] **板・窓** — サバイバーが通過できてハンターが遅延するオブジェクト

### フェーズ3: マルチプレイヤー

- [ ] **Socket.io 導入** — `npm install socket.io` して `server.js` に追加
- [ ] **ロビー・ルーム管理** — 最大5人（ハンター1+サバイバー4）のルームシステム
- [ ] **サーバー権威モデル** — 入力をサーバーに送り、状態をブロードキャスト（レイテンシー補正含む）
- [ ] **役職選択画面** — ハンター/サバイバーのロール割り当てUI

### フェーズ4: ゲームシステム

- [ ] **スキル/アビリティ** — ハンター固有能力（チェーン投擲など）・サバイバー固有スキル
- [ ] **チェアシステム** — サバイバーをチェアに拘束→仲間が救助できるメカニクス
- [ ] **ランク・マッチメイキング** — ELOレーティングシステム
- [ ] **サウンド** — BGM・SE（足音、暗号機音、悲鳴）
- [ ] **UI/HUD** — ミニマップ、暗号機進行状況、スキルクールダウン表示

### インフラ

- [ ] **環境変数管理** — `.env` + `dotenv` で PORT・DB接続情報を管理
- [ ] **データベース** — プレイヤープロフィール・統計の永続化（PostgreSQL or MongoDB）
- [ ] **本番デプロイ** — Docker化 + Railway / Render へのデプロイ設定
